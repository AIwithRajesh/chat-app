services:
  postgres:
    container_name: chatapp_db
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: chatapp
      POSTGRES_PASSWORD: chatapp_db

volumes:
  postgres_data:
